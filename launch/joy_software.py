import os
import sys

import launch
import launch_ros.actions

#NOT COMPLETE: the group tag messed up the conversion

def generate_launch_description():
    ld = launch.LaunchDescription([

    ])
    return ld


if __name__ == '__main__':
    generate_launch_description()